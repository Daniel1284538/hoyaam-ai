# misc
.DS_Store
*.pem

# env files (can opt-in for committing if needed)
.env*

# Supabase local dev
supabase/.branches
supabase/.temp

# Android app (see android/.gitignore for app-module specifics)
android/.gradle/
android/.kotlin/
android/build/
android/*/build/
android/local.properties
